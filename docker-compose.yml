services:
  wintercode:
    container_name: wintercode
    build:
      context: ./main
      dockerfile: Dockerfile
    command: sh -c "
      until nc -z mysql_wintercode 3306; do
      echo '⏳ Waiting for MySQL...';
      sleep 2;
      done &&
      npx prisma migrate deploy &&
      npx prisma generate &&
      npm run build && npm run start"
    restart: unless-stopped
    environment:
      NODE_ENV: "dev"
      PORT: 5000
      BASE_URL: "http://localhost"

      FRONTEND_URL: "http://localhost:3000"

      PAWAPAY_BASE_URL: "https://api.sandbox.pawapay.io"
      PAWAPAY_API_TOKEN: "eyJraWQiOiIxIiwiYWxnIjoiRVMyNTYifQ.eyJ0dCI6IkFBVCIsInN1YiI6IjUzMTciLCJtYXYiOiIxIiwiZXhwIjoyMDY1ODU2NzU1LCJpYXQiOjE3NTAzMjM5NTUsInBtIjoiREFGLFBBRiIsImp0aSI6IjU0YWNlYzhlLWY5ZTMtNDE5YS1iOGIwLTM0NjQ5ZGI5MzU5MiJ9.f9EpjHcwXTbXoggaD5cCbkJ9rPoRm3M1k4q2eo1lbfYS4lHMOAI-2DV_6hxpX7Ula12P6BHSoPgi5UgNDiMgdw"

      # MAIN_DATABASE_URL: "file:./dev.db"
      MAIN_DATABASE_URL: "mysql://root:krest@mysql_wintercode:3306/wintercode"
      LOG_DATABASE_URL: "mongodb://mongo_wintercode:27017/?directConnection=true&serverSelectionTimeoutMS=2000&appName=mongosh+2.5.2"
      JWT_SECRET: "your_jwt_secret"

      SMTP_USER: "info@le-carino.com"
      SMTP_PASS: "admin@carino2024"
      MYSQLDB: "mysql://root:krest@mysql:3306/wintercode"
    ports:
      - "5000:5000"
    networks:
      - app-network
    depends_on:
      - mysql_wintercode

  mongo_wintercode:
    container_name: mongo_wintercode
    image: mongo:6
    restart: unless-stopped
    environment:
      MONGO_INITDB_DATABASE: wintercode-db
    volumes:
      - mongo-data:/data/db
      # - /root/backup/wintercode/mongo-data:/data/db
    ports:
      - "27017:27017"
    networks:
      - app-network

  mysql_wintercode:
    container_name: mysql_wintercode
    image: mysql:8
    restart: unless-stopped
    environment:
      MYSQL_ROOT_PASSWORD: krest
      MYSQL_DATABASE: wintercode
      MYSQL_PASSWORD: krest
    ports:
      - "3317:3306"
    volumes:
      - mysql-data:/var/lib/mysql
      # - /root/backup/wintercode/mysql-data:/var/lib/mysql
    networks:
      - app-network

networks:
  app-network:
    driver: bridge
    attachable: true
volumes:
  mongo-data: {}
  mysql-data: {}
# correct deployment
